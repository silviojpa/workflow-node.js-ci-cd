name: Advanced CI/CD for Node.js App

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install dependencies
        run: npm ci

      - name: Lint code
        run: npm run lint

      - name: Run tests with coverage
        run: npm test
        
      - name: Build and prepare artifact
        run: |
          rm -rf dist
          mkdir dist
          cp app.js dist/
          cp app.test.js dist/
          cp package.json dist/
          cp package-lock.json dist/
          cp .gitignore dist/
          cp .eslintrc.json dist/
          echo "Build successful!"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-build-${{ github.sha }}
          path: dist

  deploy:
    runs-on: ubuntu-22.04
    needs: build # Este job só executa se o job 'build' for bem-sucedido
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: app-build-${{ github.sha }}
          path: deploy-dist

      - name: Deploy to staging server
        run: |
          echo "Deploying to staging server..."
          echo "Downloaded artifact from build job to 'deploy-dist'."
          echo "Simulating SSH/SCP to transfer files to staging."
          echo "Deployment successful!"
